<hr />
<slot></slot>

<style>
    h2 {
        text-align: center;
        color: var(--b2-fg);
        font-size: clamp(180%, 3.5vw, 40pt);
    }

    h2 hr {
        max-width: 60rem;
        margin-block: 2rem;
    }
</style>