<hr />
<slot></slot>

<style>
    h2 {
        text-align: center;
        color: var(--b2-fg);
        font-size: clamp(180%, 3.5vw, 40pt);
    }

    h2 hr {
        margin: 0.8em auto;
        max-width: 60rem;
    }
</style>